{
  "name": "nutrygym-uce",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.9.2",
  "description": "NutryGym monorepo (web + mobile) plus microservices and infrastructure. Academic project.",
  "workspaces": [
    "apps/web",
    "apps/mobile/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.7.3"
  }
}
