version: "3.9"

services:
  authorization-service:
    build: .
    container_name: authorization-service
    ports:
      - "3003:3003"
    env_file:
      - .env
    networks:
      - nutrigym-network

networks:
  nutrigym-network:
    external: true
